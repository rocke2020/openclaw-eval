## Eval Journal

Dated entries capturing what happened in each LoCoMo eval run worth remembering: scores, surprises, regressions, root causes, and lessons. One file per investigation, named `YYYY-MM-DD-<slug>.md`.

This is the analytical layer above `output/runs/`. Artifacts live there; narrative lives here.
